<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title= title
  body
    h1= title
    if likesBananas
      p this user likes bananas
    else
      p this user is not a fan of bananas
    each sub in subs
      div
        a(href=`/subbreaddits/${sub.id}`)= sub.name