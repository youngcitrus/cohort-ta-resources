// Try these out in the browser! (Inspect and go to the Console tab):
1 + 1;
const name = 'app academy';
name;

// In Node, look at the global object
// In the browser, look at the window object
// Adding a variable in each location adds it to these objects

// Node's global has access to specific data that doesn't exist in the browser, such as the process key
// Similarly, the browser's window can access the location and document

// In Node, we can use `require` to import other .js files

// In Node, we can use Node modules like 'readline'
